# JAMERYKA / Jameryka ClarityCare — Website (Correct Logo)

## What changed
- Uses your provided JAMERYKA logo in the header (`assets/logo.png`)
- Adds `favicon.ico`
- Adds compliance notice in the footer on every page
- Adds `robots.txt` and `sitemap.xml` for Google indexing readiness

## Deploy on Netlify
Drag-and-drop the contents of this folder into Netlify (Deploy manually).

## Contact form
Replace the placeholder Formspree endpoint in `contact.html`:
`action="https://formspree.io/f/yourFormID"`
with your real Formspree URL.

## Next
- Provide your Calendly link to embed bookings
- Tell me the email host for `hello@jameryka.com` so DNS can be aligned
